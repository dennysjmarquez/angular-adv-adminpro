import {defaultEnvironment} from './environment.defaults';

export const environment = {
   ...defaultEnvironment,
   production: true,
   baseUrl: 'https://adminpro-system-hospitals.herokuapp.com/api'
};
